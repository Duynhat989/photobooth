const express = require('express');
const controller= require('./controller');
const router = express.Router();
// Đăng ký Giáo viên mới
// router.get('/google/callback',  validate([]), authController.login);


module.exports = router;
